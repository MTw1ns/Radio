import 'package:flutter/material.dart';

import 'card.dart';
class grigtile extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return GridTile(
        child:
        card(text: "1"));
  }
}